# Test environment variables
# Copy to .env.test and fill in values

# Test PostgreSQL database (local Docker recommended)
# docker run -e POSTGRES_PASSWORD=prisma -p 5433:5432 -d postgres
TEST_DATABASE_URL="postgresql://postgres:prisma@localhost:5433/vaultly_test"

# Test Redis (local Docker recommended)
# docker run -p 6380:6379 -d redis
REDIS_URL="redis://localhost:6380"
