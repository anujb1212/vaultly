# For running with Docker Compose (db service):
DATABASE_URL="postgresql://postgres:prisma@db:5432/vaultly?schema=public"

# If running app directly on host (no compose DB), use:
# DATABASE_URL="postgresql://postgres:prisma@localhost:5432/vaultly?schema=public"

# Auth (NextAuth)
# App URL (adjust if needed)
NEXTAUTH_URL="http://localhost:3001"

# Generate a strong secret for local dev (do NOT commit real secrets)
NEXTAUTH_SECRET="replace-with-a-long-random-string"

# JWT
JWT_SECRET="replace-with-a-long-random-string"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"


# Webhook (used by mock-payment-gateway / bank-webhook)
WEBHOOK_SECRET="replace-with-a-long-random-string"

